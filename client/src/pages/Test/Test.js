import React from 'react'

const Test = ({ classes }) => {
    return (
        <div>
            <p>
                This is the share page located at <code>/share</code>.
        </p>
        </div>
    )
}

export default (Test)
